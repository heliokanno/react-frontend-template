# Variáveis de ambiente do frontend (Vite).
# Copie para `.env.local` e ajuste os valores. Nunca versione arquivos .env reais.

# Base URL da API. Opcional nesta fundação; passa a ser exigida na spec 004.
VITE_API_BASE_URL=
